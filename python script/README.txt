optional (necessary) arguments:
  -h, --help            show this help message and exit
  -e EXTRACT, --extract EXTRACT
                        EFI image file
  -sd SMRAMDUMP, --smramdump SMRAMDUMP
                        SMRam dump file
  -o OUTDIR, --outdir OUTDIR
                        Extract modules to the given directory
  -wl EFI_WHITELIST, --efi_whitelist EFI_WHITELIST
                        Name of json file with efi-whitelist

guid database for script:
https://github.com/abazhaniuk/efi-whitelist